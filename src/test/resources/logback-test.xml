<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{30} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Silence RestAssured's verbose internal logging; we log via ApiClient -->
    <logger name="io.restassured"              level="WARN"/>
    <logger name="org.apache.http"             level="WARN"/>
    <logger name="groovyx.net.http"            level="WARN"/>

    <!-- Framework classes log at DEBUG for full traceability -->
    <logger name="com.farmacity.bff.automation" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
